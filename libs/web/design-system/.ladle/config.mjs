// Ladle picks up vite.config.ts at the package root automatically (Tailwind
// plugin lives there) and `.ladle/components.tsx` for the global React
// provider, where the design-system CSS is imported.
export default {
  defaultStory: "typography--variants",
};
