// Layer 3 — Playwright required.
// These tests are stubs until the Playwright harness is configured.
// See INVARIANTS.md: PWA-01.

import { describe, it } from "vitest";

describe("PWA-01: browser refresh preserves active tab route", () => {
  it.todo("refreshing on /explore rehydrates to /explore with the Explore tab active");
  it.todo("refreshing on /taste rehydrates to /taste with the Taste tab active");
  it.todo("refreshing on /search rehydrates to /search with the Search tab active");
});
