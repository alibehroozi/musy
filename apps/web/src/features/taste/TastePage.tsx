import { Typography } from "@moc/design-system";

export function TastePage(): JSX.Element {
  return (
    <main className="flex items-center justify-center min-h-full p-8">
      <Typography variant="body">Taste — coming soon</Typography>
    </main>
  );
}
