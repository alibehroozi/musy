{
  "name": "moc",
  "private": true,
  "version": "0.0.1",
  "description": "Music app with AI-powered taste processing. AI-maintained, TDD.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "libs/shared/*",
    "libs/api/*",
    "libs/web/*"
  ],
  "scripts": {
    "verify": "npm run lint && npm run typecheck && npm run test",
    "verify:visual": "npm run test:visual:ds && npm run test:visual:web",
    "typecheck": "tsc --noEmit -p tsconfig.json && npm --workspaces --if-present run typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "vitest run && npm run test:ds",
    "test:watch": "vitest",
    "test:invariants": "vitest run tests/invariants",
    "test:api": "npm --workspace apps/api run test",
    "test:ds": "npm --workspace libs/web/design-system run test",
    "test:visual:ds": "npm --workspace libs/web/design-system run test:visual",
    "test:visual:ds:update": "npm --workspace libs/web/design-system run test:visual:update",
    "test:visual:web": "npm --workspace apps/web run test:visual",
    "test:visual:web:update": "npm --workspace apps/web run test:visual:update",
    "build": "npm --workspaces --if-present run build",
    "dev:api": "npm --workspace apps/api run dev",
    "dev:web": "npm --workspace apps/web run dev",
    "dev": "npm-run-all -p dev:api dev:web",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f",
    "db:reset": "docker compose down -v",
    "prepare": "husky"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^22.10.0",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-tailwindcss": "^4.0.0-beta.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.4.2",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  }
}
