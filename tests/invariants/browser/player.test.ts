// Layer 3 — Playwright required.
// These tests are stubs until the Playwright harness is configured.
// See INVARIANTS.md: BROWSER-03.

import { describe, it } from "vitest";

describe("BROWSER-03: mini-player touch targets and no horizontal scroll on mobile viewport", () => {
  it.todo(
    "on a 375×667 viewport, play/pause and dismiss buttons each have a touch target ≥ 44×44 px; no horizontal scroll",
  );
});
