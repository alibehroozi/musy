import { Typography } from "@moc/design-system";

export function ExplorePage(): JSX.Element {
  return (
    <main className="flex items-center justify-center min-h-full p-8">
      <Typography variant="body">Explore — coming soon</Typography>
    </main>
  );
}
