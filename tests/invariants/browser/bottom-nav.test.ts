// Layer 3 — Playwright required.
// These tests are stubs until the Playwright harness is configured.
// See INVARIANTS.md: BROWSER-01.

import { describe, it } from "vitest";

describe("BROWSER-01: bottom nav touch targets and iOS safe-area on mobile viewport", () => {
  it.todo(
    "on a 375×667 viewport each nav tab has a touch target ≥ 44×44 px and the nav applies env(safe-area-inset-bottom)",
  );
});
