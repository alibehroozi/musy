# Product Invariants

Properties that must **always hold**, regardless of which feature is added or changed. Invariants are **guardrails** — they fail the moment the code drifts.

## How this file is organized

**Invariants are categorized by what they constrain, not by which feature added them.** New features extend existing categories — they do not get their own sections. This is the principle that lets the file scale: a project with 200 invariants across 40 features still has the same handful of categories, and every new invariant has an obvious home.

| Prefix      | Category                                                                                 | Verified at                              |
| ----------- | ---------------------------------------------------------------------------------------- | ---------------------------------------- |
| `DATA-*`    | Shape and integrity of stored data (Mongoose docs, refs, required fields, indexes)       | Layer 2 (vitest + mongodb-memory-server) |
| `LOGIC-*`   | Pure function contracts (input → output, immutability, idempotence, round-trips)         | Layer 2 (vitest)                         |
| `API-*`     | HTTP contract: status codes, response schemas, idempotency, pagination, error shape      | Layer 2 (jest + supertest)               |
| `UI-*`      | DOM / rendering checkable in jsdom (element exists, aria reflects state, list lengths)   | Layer 2 (vitest + Testing Library)       |
| `SEC-*`     | Authorization (no IDOR, owner-only access, no PII in logs, no secrets in responses)      | Layer 2 (jest + supertest)               |
| `PRIVACY-*` | Data flow boundaries — what reaches AI prompts, third parties, telemetry                 | Layer 2 + Layer 3                        |
| `AI-*`      | Contracts around LLM/embedding calls (prompt shape, idempotent caching, dim consistency) | Layer 2                                  |
| `PWA-*`     | Manifest valid, service worker installs, offline shell loads, install prompt fires       | Layer 3 (Playwright)                     |
| `BROWSER-*` | Visual/behavioral: contrast, mobile layout without horizontal scroll, animations         | Layer 3 (Playwright)                     |

**Good invariants describe the product, not the implementation.** A solid invariant survives any rewrite — if swapping React for Solid, swapping Mongoose for Prisma, or renaming functions would invalidate it, it's a unit test in disguise. Aim for properties of stored data, observable HTTP behavior, or rendered output. Naming a specific function is OK _only_ when that function is part of the project's stable public API; never when the feature itself is creating the function.

When adding an invariant, decide what it constrains and append to the matching section. **Don't create a new per-feature section.** If nothing fits, the right move is to question the invariant — most "feature-specific" invariants reduce to one of the above when phrased as a falsifiable property.

Tests use `describe("<ID>: <description>", ...)` to map back to this file.

---

## DATA — data shape and integrity

| ID      | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Severity |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| DATA-01 | Every `User` document has a non-empty `id` (uuid v4) and a unique, lowercase `email`                                                                                                                                                                                                                                                                                                                                                                                                          | Critical |
| DATA-02 | Every `User` document has a non-empty, unique `googleId` (the Google `sub` claim)                                                                                                                                                                                                                                                                                                                                                                                                             | Critical |
| DATA-03 | Every `search_cache` document has `expiresAt` set strictly after its creation time; the TTL index drops it at `expiresAt`; `queryHash` is unique in the collection                                                                                                                                                                                                                                                                                                                            | High     |
| DATA-04 | `search_history` has a unique compound index `(userId, query)`; submitting the same normalized query twice for the same user creates exactly one document (dedupe at the DB level) and increments `searchCount`                                                                                                                                                                                                                                                                               | High     |
| DATA-05 | `interest_scores` has a unique compound index `(userId, songKey)`; any combination of explored/saved events for the same `(userId, source, externalId)` produces exactly one document                                                                                                                                                                                                                                                                                                         | Critical |
| DATA-06 | `interest_scores.score` is monotonically non-decreasing per `(userId, songKey)`: every event upsert satisfies `newDoc.score >= oldDoc.score` (max-rule)                                                                                                                                                                                                                                                                                                                                       | Critical |
| DATA-07 | `interest_scores.snapshot` is written on the first event for a given `(userId, songKey)` and is never overwritten by subsequent events on the same key                                                                                                                                                                                                                                                                                                                                        | High     |
| DATA-08 | Every `play_resolutions` document has `expiresAt` strictly after `resolvedAt`; the TTL index drops it at `expiresAt`; `snapshotHash` is unique in the collection                                                                                                                                                                                                                                                                                                                              | High     |
| DATA-09 | Every `listening_events` document has `userId`, `songKey`, `eventType ∈ {"started","completed"}`, and `at` populated, with `elapsedMs >= 0`; the collection has a compound index on `(userId, songKey, at)`                                                                                                                                                                                                                                                                                   | High     |
| DATA-10 | Every `swipes` document has `userId`, `snapshot`, `snapshotHash`, `direction ∈ {"right","left"}`, and `at` populated; the collection has compound indexes on `(userId, at)` and `(userId, snapshotHash)` and is append-only — repeating the same swipe creates a new document each time                                                                                                                                                                                                       | High     |
| DATA-11 | Every `taste_profiles` document has `userId`, `lastBuiltAt`, `swipeCountAtLastBuild`, and `summaryText` populated; the collection enforces a unique index on `userId` so each user has at most one profile. `summaryText` is unbounded at the storage layer — the LLM decides the length, and downstream consumers (the personalized prompt, AI-03's previous-summary truncation) are responsible for any byte-budget enforcement                                                             | Critical |
| DATA-12 | Every `explore_queue` document has `userId`, `items`, `phase ∈ {"discovery","artist-refinement","personalized"}`, `generatedAt`, and `swipesSeenAtBuild` populated; the collection enforces a unique index on `userId` so each user has at most one queue, replaced wholesale on refill                                                                                                                                                                                                       | Critical |
| DATA-13 | Every element of `items` in every persisted `explore_queue` document has a non-empty string `coverUrl` field. The queue is the persistence boundary for cover-less candidates: a snapshot without a resolvable artwork URL never reaches the queue document                                                                                                                                                                                                                                   | Critical |
| DATA-14 | Every `resolution_preferences` document has a non-empty string `snapshotHash` (indexed), `source === "soundcloud"`, a non-empty string `sourceTrackId`, an integer `score ≥ 1`, and a `chosenAt` Date. `(snapshotHash, source, sourceTrackId)` is a unique compound index. The collection has **no TTL index** (preferences persist forever) and **no `userId` field** (preferences are global). For any given `snapshotHash`, the document with the maximum `score` is the active resolution | Critical |
| DATA-15 | Every `buckets` document has a non-empty string `id`, `userId`, `name`; a `kind ∈ {"auto", "custom"}`; a `state ∈ {"ready", "building", "failed"}`; and `createdAt`, `lastBuiltAt` Date fields. The collection declares a compound index `(userId, id)` for scoped reads and a compound index `(userId, state)` for the polling read-path of building buckets                                                                                                                                 | Critical |
| DATA-16 | Every `bucket_song_scores` document has `userId`, `bucketId`, `songKey`, `snapshot`, and an integer `score` in `[0, 100]` populated. The collection declares a **unique** compound index `(userId, bucketId, songKey)` (one score per song per bucket per user) and a compound index `(userId, bucketId, score: -1)` for top-N ordered reads                                                                                                                                                  | Critical |

| DATA-17 | Every `context_scores` document has `userId`, `songKey`, `axis ∈ {"weekday", "timeOfDay", "month"}`, a `value` matching the closed enum for that axis (`weekday` ∈ {`mon`..`sun`}, `timeOfDay` ∈ {`morning`, `afternoon`, `evening`, `night`}, `month` ∈ {`jan`..`dec`}), an integer `score` in `[0, 100]`, a `lastEventType ∈ {"right-swipe", "left-swipe", "save", "listen-completed"}`, and a `lastEventAt` Date. The collection declares a **unique** compound index `(userId, songKey, axis, value)` (one row per user / song / context-slot) and a compound index `(userId, songKey)` for the read path that joins all axes for one song | Critical |
| DATA-18 | Every auto-built `buckets` document produced by the auto-bucket builder has `kind === "auto"` and `state === "ready"` at insert time; `name` is at most 60 characters; `description` (when present) is at most 200 characters. No duplicate names exist in the `buckets` collection for the same `userId` — name matching is case-insensitive after trim and single-space normalization (LOGIC-35). The builder never downgrades an existing `state` value (e.g. a future "failed" bucket is never written over by the auto builder) | Critical |
| DATA-19 | Every `custom_mix_jobs` document has a non-empty string `jobId` (uuid), `userId`, `bucketId`, a `promptText` of length 1..500, a `state ∈ {"building", "completed", "failed"}`, and a `startedAt` Date. `state === "failed"` requires a non-null string `errorReason`; `state === "completed"` requires a non-null `completedAt` Date. `sourceBuckets` is a `Record<songKey, string[]>` mapping every song the job assigned into the resulting bucket to the auto-bucket ids the LLM stated it drew from (may be empty `[]` for an entry the LLM did not attribute, omitted entirely while `state !== "completed"`). The collection declares a **unique** index on `jobId` and a compound index `(userId, state)` so the rate-limit guard counts in-flight jobs cheaply | Critical |
| DATA-20 | Every `buckets` document with `kind === "custom"` has a non-empty string `promptText` (≤ 500 chars); every `buckets` document with `kind === "auto"` has `promptText === null`. A custom-mix bucket in `state === "building"` carries the user's submitted prompt verbatim in `promptText`; its `name` is seeded from the prompt (trimmed and truncated to 60 chars, with `"Custom mix"` as the fallback when the prompt is whitespace-only) so DATA-15's non-empty-name invariant holds during the build window, then is overwritten by the LLM-supplied name when the bucket flips to `state === "ready"`. A custom-mix bucket in `state === "failed"` carries a non-null `errorReason`. The custom-mix builder never writes a `kind === "auto"` row, and the auto-bucket builder (DATA-18) never writes a `kind === "custom"` row | Critical |
| DATA-21 | Every `listening_events` document written by `POST /api/play/started` or `POST /api/play/completed` satisfies the invariant `(bucketId === null) === (bucketKind === null)`: both fields are null together or both are non-null together. When the play originated in a bucket, `bucketId` is the UUID of that bucket and `bucketKind ∈ {"auto", "custom"}`. When the play originated outside a bucket (Search, Explore, direct resolve), both fields are null. A document whose `bucketId` is non-null but `bucketKind` is null (or vice-versa) is structurally invalid and must never be written | High |

**Test files:** `tests/invariants/data/users.test.ts`, `tests/invariants/data/search.test.ts`, `tests/invariants/data/interest-scores.test.ts`, `tests/invariants/data/play.test.ts`, `tests/invariants/data/listening-events.test.ts`, `tests/invariants/data/swipes.test.ts`, `tests/invariants/data/taste-profiles.test.ts`, `tests/invariants/data/explore-queue.test.ts`, `tests/invariants/data/buckets.test.ts`, `tests/invariants/data/bucket-song-scores.test.ts`, `tests/invariants/data/context-scores.test.ts`, `tests/invariants/data/auto-bucket-builder.test.ts`, `tests/invariants/data/custom-mix-jobs.test.ts`, `tests/invariants/data/skip-tracking.test.ts`

---

## LOGIC — pure function contracts

| ID           | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Severity |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| LOGIC-01     | The search aggregator's dedupe + merge function is deterministic: given the same list of provider results, it always produces the same output regardless of how many times it is called                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | High     |
| LOGIC-02     | The `withTimeout` helper resolves within `timeout + 100ms` even when the wrapped promise never settles                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | High     |
| LOGIC-03     | Dedupe collapses results that share an ISRC, or whose normalized title + artist are within a Levenshtein distance of 3, into a single result whose `sources` array lists every contributing provider                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | High     |
| LOGIC-04     | The web-core `searchTracks` fetcher validates the API response against the `SearchResponse` Zod schema and throws a `ZodError` when the response body does not match the schema                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | High     |
| LOGIC-05     | The `computeSnapshotHash` function is stable: equal `(title, artist, durationSec)` triples — modulo leading/trailing whitespace and ASCII case in `title` and `artist` — produce equal SHA-256 hashes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | High     |
| LOGIC-06     | The pure SoundCloud parser `extractSourceFromHtml(html)` is deterministic: the same HTML string always produces the same parsed `(sourceTrackId, clientId, transcodings)` result (or `null`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | High     |
| LOGIC-07     | The pure helper `bumpScore(oldScore, eventType)` returns `max(oldScore, 3)` for `"started"` and `max(oldScore, 5)` for `"completed"`; the result is never less than `oldScore` and is deterministic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | High     |
| LOGIC-08     | The audio engine in `libs/web/core/player/audio-engine.ts` is a deterministic state machine: given an event sequence (`load`, `playing`, `pause`, `ended`, `error`, …), the resulting `(status, currentTrack, progressMs)` triple is stable and testable without a real `<audio>` element (mock the driver interface)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | High     |
| LOGIC-09     | The web-core `resolveAndPlay(snapshot, apiBase)` function validates the `/play/resolve` response against the `ResolveResponse` Zod schema and propagates `ZodError` when the response body does not conform                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | High     |
| LOGIC-10     | The pure helper `isPlayableTranscoding({protocol, snipped})` is deterministic: returns `true` only for non-snipped `progressive` or `hls` protocols; returns `false` for any snipped variant or any protocol whose name contains `encrypted` (e.g. `cbc-encrypted-hls`, `ctr-encrypted-hls`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | High     |
| LOGIC-11     | The pure helper `formatProgress(currentMs, durationMs)` is deterministic and total: it returns `{ fraction, currentLabel, remainingLabel }` where `fraction` is in `[0, 1]`; `durationMs <= 0`, NaN, or non-finite inputs collapse to `{ fraction: 0, currentLabel: "0:00", remainingLabel: "-0:00" }`; `currentMs >= durationMs` clamps `fraction` to `1` and `remainingLabel` to `"-0:00"`; labels follow the `m:ss` (or `h:mm:ss` ≥ 1h) shape                                                                                                                                                                                                                                                                                                                                                                                                                            | High     |
| LOGIC-12     | The audio engine's `seek(positionMs)` method calls the driver with the equivalent seconds, updates `progressMs` to the requested target, never advances `progressMs` to negative or beyond `durationMs`, emits exactly one `stateChange`, and is a no-op when no track is loaded                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | High     |
| LOGIC-13     | The pure SoundCloud search-hit normalizer is deterministic: the same raw search hit always produces the same `TrackResult` (or `null`); whenever it produces a non-null result, that result satisfies `provider === "soundcloud"` and `sources` is exactly `["soundcloud"]`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | High     |
| LOGIC-14     | The pure helper `bumpScore`, extended to accept swipe event types, returns `max(oldScore, 8)` for `"swiped_right"` and `oldScore` for `"swiped_left"`; the result is never less than `oldScore` and the function is deterministic (extends LOGIC-07)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | High     |
| LOGIC-15     | The pure helper `phaseFor(profile, totalSwipeCount)` is deterministic and total: `null` profile → `"discovery"`; profile present with < 8 strong-signal artists → `"artist-refinement"`; otherwise `"personalized"`. Profile existence — not genre-count thresholds — is the discovery exit gate. Output depends only on its arguments (no `Date.now()`, no I/O)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | High     |
| LOGIC-16     | The pure helper `classifyByListenCount(listenCount)` is deterministic: `null` or `< NICHE_THRESHOLD` → `"niche"`; `>= COMMON_THRESHOLD` → `"common"`; values in between → `"mid"`. The thresholds are constants and the helper never calls a model                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | High     |
| LOGIC-17     | The pure helper `directionFromDrag({ dx, dy, threshold })` is deterministic and total: `dx >= threshold` → `"right"`; `dx <= -threshold` → `"left"`; otherwise `null`. Output depends only on its arguments (no DOM, no `Date.now()`, no I/O); `dy` does not change the result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | High     |
| LOGIC-18     | The pure cover-resolution helper in `libs/api/core/explore/` is deterministic and total: given a list of `SongSnapshot` candidates and a `(title, artist) → TrackResult \| null` lookup, it returns only snapshots whose `coverUrl` is a non-empty string. Every input snapshot with an existing non-empty `coverUrl` appears in the output in its original order with that `coverUrl` preserved; every input snapshot without a `coverUrl` for which the lookup returns a track with a non-empty `artworkUrl` appears with that `artworkUrl` as its `coverUrl`; every input snapshot without a `coverUrl` for which the lookup returns `null` or a track without `artworkUrl` is absent from the output. Output depends only on its arguments (no I/O)                                                                                                                     | High     |
| LOGIC-19     | Every HTTP request issued by a `libs/web/core` API-client function (the shared `fetchJson` helper and every sibling fetcher under `libs/web/core/src/` — `searchTracks`, `getSearchHistory`, `resolveStream`, `recordPlayStarted`, `recordPlayCompleted`, `recordExplored`, `recordSaved`, `fetchNext`, `fetchProfile`, `submitSwipe`) is sent with `credentials: "include"` in its `RequestInit`. This is the property that lets the browser attach the session cookie to every API call regardless of whether web and API share an origin in production, so authenticated endpoints stop silently 401-ing on cross-origin deployments                                                                                                                                                                                                                                     | Critical |
| LOGIC-20     | `parseColdStartResponse` is tolerant of the wrappers LLMs add around JSON output: it extracts the first balanced `{ … }` object from its input (skipping leading prose, markdown code fences such as ` ```json ` / ` ``` `, and trailing text) and parses that. The function is deterministic and total — it never throws. Inputs containing a valid `{"songs":[{title,artist}, …]}` object produce the same list of suggestions whether the object is bare, fenced, or surrounded by prose; inputs with no valid JSON object or no `songs` array return `[]`. This closes the production failure where Haiku-generated cold-start output (wrapped in ` ```json … ``` ` fences) was silently producing zero suggestions and surfacing as `explore_cold_start_failed: cold-start: empty response`                                                                            | Critical |
| ~~LOGIC-21~~ | **Retired (superseded by LOGIC-24).** Was: `parseRerankResponse` JSON-tolerance contract. The personalized phase no longer uses the old `{"ranked":[...]}` shape; the rerank prompt + parser were replaced by `buildPersonalizedPrompt` / `parsePersonalizedResponse` whose response is `{"picks_from_pool":[...], "novel_suggestions":[...]}`. LOGIC-24 carries forward the same wrapper-tolerance contract for the new parser                                                                                                                                                                                                                                                                                                                                                                                                                                             | —        |
| LOGIC-22     | `parseTasteProfileResponse` lives in `@moc/api-core` (pure logic, no NestJS deps) and applies the same LLM-JSON-wrapper tolerance as LOGIC-20/21 — extracts the first balanced `{ … }` object from its input (skipping prose, markdown code fences, trailing text), parses it, and validates the result against the `TasteProfileLLMOutput` Zod schema. Determinism preserved; unlike LOGIC-20/21 the function **throws** on failure (no JSON found, malformed JSON, schema mismatch) because the caller cannot proceed without a valid profile — the throw flows into the existing `taste_profile_build_failed` log path and the original `err` detail is preserved. Closes the production failure where Haiku-wrapped output (`SyntaxError: Unexpected token '\`', \`\`\`json …`) was being logged as `llm_parse_failed` and silently aborting taste-profile builds       | Critical |
| LOGIC-23     | The pure helper `playableHandoffDecision({ progressMs, durationMs, lookaheadMs })` is deterministic and total: returns `true` iff `Number.isFinite(durationMs) && Number.isFinite(progressMs) && durationMs > 0 && progressMs >= 0 && (durationMs - progressMs) <= lookaheadMs && (durationMs - progressMs) > 0`; otherwise returns `false`. `lookaheadMs <= 0`, NaN, or non-finite inputs collapse to `false`. Output depends only on its arguments (no `Date.now()`, no I/O); the helper underpins UI-22's "the current track is about to end — refresh the next cached URL now" decision so the queue always hands off to a freshly-signed stream URL                                                                                                                                                                                                                    | High     |
| LOGIC-24     | `parsePersonalizedResponse` lives in `@moc/api-core` (pure logic, no NestJS deps) and shares the same wrapper-tolerance contract as LOGIC-20 / LOGIC-22: extracts the first balanced `{ … }` object (skipping leading prose, markdown code fences, and trailing text) and parses that. Deterministic and total — never throws. Inputs containing a valid `{"picks_from_pool":[{title,artist}, …], "novel_suggestions":[{title,artist}, …]}` object produce the same `(picks, novel)` lists whether the object is bare, fenced, or surrounded by prose; malformed entries inside either array are dropped silently and the well-formed ones survive; inputs with no valid JSON object, missing either array, or no parseable items return `{ picks_from_pool: [], novel_suggestions: [] }` — never throws. Successor to retired LOGIC-21                                     | Critical |
| LOGIC-25     | The pure helper `buildPersonalizedPrompt({ profile, scoreBuckets, candidatePool })` produces a `(system, userMessage)` pair handed to the Anthropic SDK for the personalized phase. The system prompt instructs the model to (a) pick 10 tracks from `candidatePool`, (b) generate 10 novel suggestions consistent with the profile that are **not** in any of the `scoreBuckets` (the user's already-rated history). The user-message JSON body carries the projected `profile` (5 randomly-shuffled artists with scores, top genres with scores, `summaryText`, `tempoBucket`, `remixPreference`), the three bucket arrays (each `{title, artist}` only), and the `candidatePool` (`title, artist, source`). Deterministic given identical inputs (the artist-shuffle is the caller's responsibility, not this builder's) — equal arguments produce byte-identical output | Critical |
| LOGIC-26     | The pure helper `buildArtistRefinementPrompt({ profile, candidatePool })` produces a `(system, userMessage)` pair handed to the Anthropic SDK for the artist-refinement phase. The system prompt instructs the model to pick exactly `N=20` tracks from `candidatePool` that best fit the profile — verbatim `(title, artist)` pairs from the pool, no paraphrasing, no novel generation. The user-message JSON body carries the projected `profile` (`genres`, `artists` with scores, `summaryText`, `tempoBucket`, `remixPreference`) and the `candidatePool` (`title, artist, source`). Identity-free and deterministic — equal arguments produce byte-identical output                                                                                                                                                                                                  | Critical |
| LOGIC-27     | `parseArtistRefinementResponse` lives in `@moc/api-core` and shares the same wrapper-tolerance contract as LOGIC-20 / LOGIC-22 / LOGIC-24: extracts the first balanced `{ … }` object (skipping leading prose, markdown code fences, and trailing text) and parses that. Deterministic and total — never throws. Inputs containing a valid `{"picks":[{title,artist}, …]}` object produce the same picks list whether the object is bare, fenced, or surrounded by prose; malformed entries inside the array are dropped silently and the well-formed ones survive; inputs with no valid JSON object, missing `picks`, or no parseable items return `{ picks: [] }` — never throws                                                                                                                                                                                          | Critical |
| LOGIC-28     | `buildColdStartPrompt({ recentSwipes? })` is a pure helper. With no input, with `{}`, or with `{ recentSwipes: [] }` it produces output **byte-identical** to the pre-soft-signal version (cache-key compat: the legacy first-call cold-start hit doesn't move). When `recentSwipes` is non-empty, the system prompt gains a soft-signal section instructing the model to lean toward right-swiped feel and away from left-swiped feel while explicitly NOT forbidding any artist (same artist with a different track is OK if it fits the overall direction), and the user message carries the projected swipes `{title, artist, direction}` only. Recent swipes are truncated newest-first to `COLD_START_MAX_RECENT_SWIPES` (50). Deterministic — equal inputs produce byte-identical output                                                                             | Critical |

| LOGIC-29 | The pure helpers `bucketWeekday(date)`, `bucketTimeOfDay(date)`, and `bucketMonth(date)` live in `@moc/api-core` and are deterministic and total over every `Date` value. `bucketWeekday` returns one of `"mon".."sun"` matching the date's `getDay()`; `bucketMonth` returns one of `"jan".."dec"` matching `getMonth()`; `bucketTimeOfDay` partitions the local hour into `"night"` for `[0, 6)`, `"morning"` for `[6, 12)`, `"afternoon"` for `[12, 18)`, `"evening"` for `[18, 24)` — boundary hours `0`, `6`, `12`, `18` map to the **later** slot they start. Output depends only on the input `Date` (no `Date.now()`, no I/O) | High |
| LOGIC-30 | The pure helper `scoreDelta(eventType)` returns an `{ op: "inc", delta: number } \| { op: "set", value: number }` shape per the contextual-scoring rule: `"right-swipe"` → `{op: "inc", delta: 10}`, `"save"` → `{op: "inc", delta: 15}`, `"listen-completed"` → `{op: "inc", delta: 5}`, `"left-swipe"` → `{op: "set", value: 0}`. The helper is total over all four event types, deterministic, and the only place the delta table is encoded | High |
| LOGIC-31 | The pure helper `clampScore(rawScore)` is deterministic and total: any finite input is clamped to the integer range `[0, 100]` (values `> 100` collapse to `100`, values `< 0` collapse to `0`, non-integer values are rounded toward zero); non-finite inputs (`NaN`, `±Infinity`) collapse to `0`. The helper is the single source of truth for the contextual-score clamping rule, so every `context_scores` row written through it stays within `[0, 100]` | High |
| LOGIC-32 | The pure helper `generalScore(contextRows, bucketRows)` lives in `@moc/api-core` and is deterministic and total. With both inputs empty it returns `0`. Otherwise it computes the arithmetic mean of the four axes' scores (`weekday`, `timeOfDay`, `month`, `bucket`) — for each axis it averages the `score` field of the rows that belong to that axis, treating an axis with zero rows as contributing `0`. The result is rounded to the nearest integer and clamped to `[0, 100]`. Output depends only on its arguments (no I/O); the helper is the single place the at-request-time ranking score is computed | Critical |
| LOGIC-33 | The pure helper `isEligibleAtSlot({ snapshotHash, swipeHistory, currentSlot })` lives in `@moc/api-core` (`libs/api/core/src/explore/eligibility.ts`) and is deterministic and total over all input shapes. Returns `true` iff `swipeHistory` contains no entry whose `snapshotHash` equals the input hash AND whose `(weekday, timeOfDay)` derived via `bucketWeekday(at)` / `bucketTimeOfDay(at)` equals `currentSlot.weekday` / `currentSlot.timeOfDay`; otherwise `false`. An empty `swipeHistory` returns `true`. Both swipe directions (`right` and `left`) contribute equally to slot-burn (a left-swipe burns the slot just like a right-swipe — the user has judged the song in that context). A swipe whose `at` is missing, not a `Date`, or yields `NaN` from `getTime()` is treated as **all-slots-burnt** for that snapshot's hash (the helper returns `false` whenever such a swipe matches the hash, regardless of `currentSlot`) — defensive against malformed historical rows. Output depends only on its arguments; the helper performs no I/O and never throws | Critical |
| LOGIC-34 | A `bucket_song_scores` row inserted by the auto-bucket builder carries the LLM-supplied `initialScore` clamped to `[0, 100]` (via `clampScore`). A row that already exists for `(userId, bucketId, songKey)` is **not** overwritten by the builder — it uses an upsert with `$setOnInsert` so only the first insertion seeds the score. Subsequent score changes come exclusively from the feature 02 event-scoring path. Running the builder twice with identical inputs produces no duplicate rows and leaves all existing `score` values unchanged | Critical |
| LOGIC-35 | The pure helper `normalizeBucketName(name: string)` lives in `@moc/api-core` (`libs/api/core/src/taste/normalize-bucket-name.ts`) and is deterministic and total. It trims leading/trailing whitespace, collapses internal runs of whitespace to a single space, and lowercases the result. The output is the equality key used when checking whether a bucket name the LLM proposes already exists in the user's bucket collection — two names that are identical after normalization are treated as the same bucket (e.g. `"Late Night Drives"` and `"  late  night  drives  "` normalize to `"late night drives"` and are considered identical). The helper never throws; empty-string input returns empty string | High |
| LOGIC-36 | The pure helper `isSkip({ playedMs, durationMs })` lives in `@moc/api-core` (`libs/api/core/src/play/is-skip.ts`) and is deterministic and total. Returns `true` iff `playedMs < 30_000 AND playedMs / durationMs < 0.5`; otherwise `false`. Boundary cases: `playedMs === 30_000` returns `false`; `playedMs / durationMs === 0.5` returns `false`. Output depends only on its arguments (no I/O, no side effects). The helper is the single source of truth for the skip threshold — every skip-detection code path calls it rather than inlining the condition | High |
| LOGIC-37 | Skip-score decrements apply iff `bucketKind === "custom"` AND a matching `custom_mix_jobs` row with `state === "completed"` exists for that `bucketId`. Plays originating in auto-buckets, Search, Explore, or any non-`"custom"` surface never trigger a decrement. When `bucketKind === "custom"` but no completed `custom_mix_jobs` row exists (e.g. the row is still `"building"` or was never created), the decrement is skipped and `skip_attribution_missing_job_row` is logged — no crash, no score change | High |
| LOGIC-38 | The pure helper `selectUnbucketedPool({ pool, scoredSongKeys, cap })` lives in `@moc/api-core` (`libs/api/core/src/taste/select-unbucketed-pool.ts`) and is deterministic and total. Given a newest-first ordered `pool` of positive-signal songs and a `scoredSongKeys: Set<string>` enumerating every `songKey` that already has at least one `bucket_song_scores` row for the user, returns the first `cap` entries of `pool` whose `songKey` is **not** in `scoredSongKeys`. `cap` defaults to `20`. The pool ordering is preserved; entries already in `scoredSongKeys` are skipped without being counted toward `cap`. Empty `pool` and a `pool` whose every entry is in `scoredSongKeys` both return `[]`. Output depends only on its arguments; the helper performs no I/O and never throws. `BucketBuilderService.runBuild` uses this helper to drive its **incremental** policy: each run only sends to the LLM songs the bucket-builder has not yet considered, so the per-run output stays small and `auto_bucket_build_failed reason=llm_parse_failed` from `max_tokens` truncation is structurally avoided. When the helper returns `[]` the service short-circuits before any Anthropic call | Critical |

**Test files:** `tests/invariants/logic/search.test.ts`, `tests/invariants/logic/search-web.test.ts`, `tests/invariants/logic/play.test.ts`, `tests/invariants/logic/player.test.ts`, `tests/invariants/logic/now-playing.test.ts`, `tests/invariants/logic/explore.test.ts`, `tests/invariants/logic/explore-web.test.ts`, `tests/invariants/logic/playable-handoff-decision.test.ts`, `tests/invariants/logic/web-core-credentials.test.ts`, `tests/invariants/logic/skip-tracking.test.ts`, `libs/api/core/src/explore/cold-start-prompt.test.ts`, `libs/api/core/src/explore/personalized-prompt.test.ts`, `libs/api/core/src/explore/artist-refinement-prompt.test.ts`, `libs/api/core/src/explore/taste-prompt.test.ts`, `libs/api/core/src/explore/eligibility.test.ts`, `libs/api/core/src/taste/time-buckets.test.ts`, `libs/api/core/src/taste/score-deltas.test.ts`, `libs/api/core/src/taste/general-score.test.ts`, `libs/api/core/src/taste/bucket-prompt.test.ts`, `libs/api/core/src/taste/normalize-bucket-name.test.ts`, `libs/api/core/src/taste/select-unbucketed-pool.test.ts`

---

## API — HTTP contract

| ID     | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Severity |
| ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| API-01 | Every error response from `apps/api` matches the shared `ErrorResponse` Zod schema                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Critical |
| API-02 | `GET /api/auth/me` returns 401 + `ErrorResponse` with no/invalid session cookie; returns 200 + a body matching the `User` Zod schema with a valid session cookie                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Critical |
| API-03 | `POST /api/search` is publicly accessible (no session required); returns 400 + `ErrorResponse` when `q` is empty or missing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Critical |
| API-04 | `POST /api/search` always returns 200 with a body matching `SearchResponse` (`results`, `partial`, `failedProviders`, `cached`), even when all providers fail (`results: []`, `partial: true`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Critical |
| API-05 | `GET /api/search/history` requires a valid session cookie; returns 401 + `ErrorResponse` when no session is present                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Critical |
| API-06 | Cursor pagination on `GET /api/search/history` is stable: issuing the same cursor twice returns the same page of entries (no skipped or duplicated entries when the underlying data has not changed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | High     |
| API-07 | `POST /api/search/explored` and `POST /api/search/saved` return 401 + `ErrorResponse` without a valid session cookie; with a valid cookie and a body matching the request schema they return 204                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Critical |
| API-08 | `POST /api/play/resolve` is publicly accessible (no session required); returns 400 + `ErrorResponse` when the body is missing or fails the `ResolveRequest` schema; never returns 404 for a snapshot the providers can't match                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Critical |
| API-09 | `POST /api/play/resolve` always returns 200 with a body matching `ResolveResponse`, including the case where every provider yields nothing (`{ source: null, sourceTrackId: null, streamUrl: null, expiresAt: null }`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Critical |
| API-10 | `POST /api/play/started` and `POST /api/play/completed` return 401 + `ErrorResponse` without a valid session cookie; with a valid cookie and a body matching the request schema they return 204 with no body                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Critical |
| API-11 | `POST /api/play/started` and `POST /api/play/completed` return 400 + `ErrorResponse` when the request body fails the `PlayStartedRequest` / `PlayCompletedRequest` schema; the server never reads `userId` from the body                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Critical |
| API-12 | When `POST /api/play/resolve` returns a non-null `streamUrl`, that URL points to a stream playable by every standards-compliant browser without DRM; concretely it never resolves to SoundCloud's encrypted pipeline (host `playback.media-streaming.soundcloud.cloud` or path containing `/cbcs/` or `/cenc/`). When the strict-best SoundCloud match has only DRM-encrypted transcodings, the resolver falls through to a candidate with a non-DRM transcoding (or returns `streamUrl: null` if no playable candidate exists)                                                                                                                                                                                                                                                                                                  | Critical |
| API-13 | When the SoundCloud provider call rejects or times out during `POST /api/search`, the response's `failedProviders` array contains the literal `"soundcloud"`; when SoundCloud responds successfully (whether with zero or many results) `failedProviders` does not contain `"soundcloud"`. The response body always continues to match the `SearchResponse` Zod schema regardless of SoundCloud's outcome                                                                                                                                                                                                                                                                                                                                                                                                                        | Critical |
| API-14 | `POST /api/explore/swipe` returns 401 + `ErrorResponse` without a valid session cookie; with a valid cookie and a body matching `SwipeRequest`, returns 204 with no body; returns 400 + `ErrorResponse` when the body fails the schema. A right-swipe upserts an `interest_scores` document with `score >= 8` for the same user; a left-swipe leaves `interest_scores` unmodified                                                                                                                                                                                                                                                                                                                                                                                                                                                | Critical |
| API-15 | `GET /api/explore/profile` returns 401 + `ErrorResponse` without a valid session cookie; with a valid session it returns 200 with a body matching `TasteProfileResponse` — `null` for users below the build threshold, otherwise an object matching `TasteProfile`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Critical |
| API-16 | `GET /api/explore/next` returns 401 + `ErrorResponse` without a valid session cookie; with a valid session returns 200 with a body matching `NextResponse` (`items`, `phase`, `partial`, `buildingQueue`); never 5xx-es when only the rerank LLM path fails — degrades to heuristic top-N. `count` is clamped to `[1, 50]` and defaults to 20                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Critical |
| API-17 | `GET /api/explore/next` response: every element of the `items` array has `coverUrl` set to a non-empty string. The endpoint never returns a `SongSnapshot` with a missing or empty `coverUrl`, even when the underlying queue or downstream filters would otherwise yield one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Critical |
| API-18 | The queue refill trigger fires on the count of **slot-eligible** items remaining (per API-25), not the raw `items.length` of the stored queue document. After each swipe write, the refill check counts queue items for which `isEligibleAtSlot({ snapshotHash, swipeHistory, currentSlot })` (LOGIC-33) is `true` against the user's full swipe history at the **current** `(weekday, timeOfDay)` slot, and triggers `rebuildQueue` (fire-and-forget) when that count is `< REFILL_THRESHOLD` (5). Closes the production failure where the queue document stayed at length 20 (because swipes are append-only to a separate collection and never prune the queue), so `maybeRefill` never observed a count below threshold and `GET /api/explore/next` started returning `{items:[], partial:true}` once all 20 had been swiped | Critical |
| API-19 | When `rebuildQueue` runs and the user is in the discovery phase with `totalSwipeCount >= SWIPE_TRIGGER_THRESHOLD` (20) and no profile has been built yet (or a build is in flight), the rebuild **awaits** the profile build before computing the new phase. Concretely: `ProfileBuilderService` exposes an awaitable `buildIfDue(userId)` that resolves once the in-flight or newly-started build completes (or no-ops if not due), and `rebuildQueue` calls it before re-reading the profile and calling `phaseFor`. This ensures the post-cold-start refill exits discovery in a single round-trip instead of looping on stale `profile === null` reads while the fire-and-forget build is mid-flight                                                                                                                         | Critical |
| API-20 | `GET /api/explore/next` response carries a `buildingQueue: boolean` field. It is `true` iff a queue rebuild is currently in flight for the user (or has just been triggered by this very call) at the moment the response is computed; `false` otherwise. Concretely: `QueueBuilderService` exposes whether `inFlightRebuilds.has(userId)` and the controller emits that value. Lets the UI distinguish "loading — more tracks are coming" from "genuinely empty / failed" so it can poll without re-triggering rebuilds (UI-23)                                                                                                                                                                                                                                                                                                 | Critical |
| API-21 | `QueueBuilderService.rebuildQueue(userId)` is idempotent per user: concurrent invocations for the same `userId` resolve to the same single underlying build (the second caller awaits the first's promise) and never produce two parallel candidate-sourcing pipelines / LLM calls. Implemented via `inFlightRebuilds: Map<userId, Promise<void>>`; the entry is cleared on settle. `getNext` reads this map to set `buildingQueue` and to short-circuit firing a duplicate rebuild when one is already running. The polling cadence from UI-23 therefore never multiplies upstream cost                                                                                                                                                                                                                                         | Critical |
| API-22 | `POST /api/play/reresolve` requires a valid session (401 + `ErrorResponse` without one); validates the body against `ReresolveRequest` (400 + `ErrorResponse` on schema failure); returns 200 with a body matching `ResolveResponse`. On success, the persisted `resolution_preferences` document has `score = (current max score for that snapshotHash) + 1` (1 when none existed). The candidate picker never selects a `sourceTrackId` that already has a preference document for that hash; among the remaining un-tried SoundCloud candidates, it picks the one with the highest `playback_count`. When no further candidate exists, the endpoint returns `{ source: null, sourceTrackId: null, streamUrl: null, expiresAt: null }` and writes no document                                                                  | Critical |
| API-23 | When `resolution_preferences` has at least one document for the snapshotHash derived from a `POST /api/play/resolve` request, the response's `source` and `sourceTrackId` come from the **highest-score** preference document for that hash — the request makes no upstream provider call and ignores the `play_resolutions` cache for those two fields. When no preference document exists, the existing cache + upstream resolution path (API-08 / API-09 / API-12) runs unchanged                                                                                                                                                                                                                                                                                                                                             | Critical |

| API-24 | `GET /api/me/taste/profile` returns 401 + `ErrorResponse` without a valid session cookie; with a valid session it returns 200 with a body matching `TasteBucketsResponse` (`{ buckets: TasteBucket[] }`). The response never carries Mongo internals (`_id`, `__v`) and never exposes `userId` fields belonging to a different session. For a user with no `buckets` documents the response body is exactly `{ buckets: [] }` — never `null`, never an empty body. The contract is named `TasteBucketsResponse` (not `TasteProfileResponse`) because the existing `TasteProfileResponse` in `@moc/contracts` belongs to the explore-tab's LLM-derived genre/artist profile | Critical |
| API-25 | `GET /api/explore/next` excludes a candidate iff at least one of the authenticated user's `swipes` for that snapshot's hash lands in the current `(weekday, timeOfDay)` slot (derived from server-side `new Date()` via `bucketWeekday` / `bucketTimeOfDay` from `@moc/api-core`). Concretely the response-filter step uses `isEligibleAtSlot({ snapshotHash, swipeHistory, currentSlot })` (LOGIC-33) per candidate against the user's full `swipes` list. Both swipe directions burn the slot equally — once a song is swiped in a slot, that slot stays burnt for that song. There are exactly 28 possible slots (7 weekdays × 4 time-of-day buckets); a song with at least one swipe in every slot is permanently excluded. **Month is not an eligibility axis** — it influences scoring (DATA-17) only. The first-run case (no swipes for the user) is a no-op filter — every candidate is eligible. Replaces the prior "any swipe excludes forever" implicit rule embedded in `seenHashes` set logic, per AGENTS.md hard rule #6's explicit-approval clause (this PR is the approval). The rule also gates the queue-build candidate-sourcing pipeline's dedupe step: cold-start / artist-refinement / personalized sourcing dedupes new candidates against slot-burnt entries only, not all swiped entries | Critical |
| API-26 | `POST /api/me/taste/custom-mix` requires a valid session (401 + `ErrorResponse` without one); validates the body against `CustomMixRequest` and returns 400 + `ErrorResponse` when `promptText` is missing / empty after trim / > 500 chars; returns 422 + `ErrorResponse` when the authenticated user's positive-signal pool (right-swiped ∪ saved ∪ listen-completed, deduped by songKey) is empty. On success returns 200 with a body matching `CustomMixCreatedResponse = { jobId, bucketId }` — both uuid v4 strings — **before** the Anthropic call is issued. The controller method resolves the HTTP response in a microtask of the pre-insert (the LLM build is fire-and-forget via `Promise.catch`), so the response never blocks on Anthropic. Immediately after the 200, `GET /api/me/taste/profile` lists the new bucket with `kind: "custom"`, `state: "building"`, `promptText` set to the submitted prompt | Critical |
| API-27 | `POST /api/me/taste/custom-mix` enforces a hard cap of 5 concurrent in-flight custom-mix builds per `userId`: the 6th request (while five jobs remain in `state: "building"` for that user) returns 429 + `ErrorResponse` and writes no `buckets` or `custom_mix_jobs` row. The cap is checked **before** the pre-insert via an in-process `Map<userId, Set<jobId>>` joined with a `custom_mix_jobs.countDocuments({ userId, state: "building" })` (DATA-19's `(userId, state)` index covers this) so cross-process replicas converge. A job finishing — whether `state` flips to `"completed"` or `"failed"` — releases its slot, after which the next request returns 200. The cap is per-user; one user's saturation never affects another | Critical |

**Test files:** `tests/invariants/api/auth.test.ts`, `tests/invariants/api/search.test.ts`, `tests/invariants/api/play.test.ts`, `tests/invariants/api/explore.test.ts`, `tests/invariants/api/explore-queue-builder.test.ts`, `tests/invariants/api/taste.test.ts`, `tests/invariants/api/custom-mix.test.ts`

---

## UI — DOM / rendering, checkable in jsdom

| ID    | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Severity |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| UI-01 | The app shell renders a routed bottom navigation for all users regardless of authentication state; the sign-in flow is not shown at the root shell level                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | High     |
| UI-02 | The bottom navigation is visible on every routed page (`/explore`, `/taste`, `/search`, and the not-found fallback) regardless of authentication state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | High     |
| UI-03 | Exactly one bottom-nav tab carries `aria-current="page"` at any time, matching the current route path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | High     |
| UI-04 | When the search input is empty and there is no per-user history, the suggestions block ("Try: …") is visible in the results area                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | High     |
| UI-05 | When a search request is in flight, a skeleton loading indicator is visible in the results area; previous results are replaced by the skeleton                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | High     |
| UI-06 | On a successful response with non-empty results, every result in the response is rendered as a `ResultRow`; track rows show title+artist and station rows show a "Live" indicator                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | High     |
| UI-07 | When the authenticated user has ≥ 1 search history entry and the input is empty, the history list is visible in the results area and the static suggestions block is not rendered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | High     |
| UI-08 | Pressing Enter in the search input on `/search` removes focus from the input element (so the on-screen keyboard dismisses on touch devices); the active element after Enter is no longer the search input                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Medium   |
| UI-09 | When an unauthenticated user taps a result row OR the row's add button, the sign-in Modal opens and no `POST /api/search/explored` or `POST /api/search/saved` request is fired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Critical |
| UI-10 | The sign-in Modal sits at z-index `--z-modal` so it renders above the fixed bottom navigation; on a 375×667 viewport the modal's content is not occluded by the nav                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | High     |
| UI-11 | When a track is playing or paused mid-track, the mini-player is rendered above the bottom nav on non-explore routed pages (`/taste`, `/search`); when no track has ever been played the mini-player is absent; the mini-player is **never** rendered on `/explore` regardless of playback state (the explore card stack owns the player surface)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | High     |
| UI-12 | When a track resolves to `source: null` (no stream), the mini-player renders in the failed state (visible warning + dismiss button) and no `<audio>` src is set                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Critical |
| UI-13 | The currently-playing search result row renders with a `data-playing="true"` attribute; all other rows do not have that attribute                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | High     |
| UI-14 | When the player's `isExpanded` flag is `true` and a track is loaded, the now-playing overlay is rendered with `role="dialog"` and `aria-modal="true"`; when `isExpanded` is `false` or no track is loaded, the overlay is not in the DOM                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | High     |
| UI-15 | The now-playing overlay renders the **station** variant (LIVE indicator visible, no progress bar, both skip buttons carry `aria-disabled="true"`) when `currentTrack.snapshot.kind === "station"`, and the **track** variant (progress bar visible, skip-back button enabled) when `kind === "track"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | High     |
| UI-16 | The docked mini-player is **never** rendered on the `/explore` route — the explore card stack is the sole player surface regardless of what `currentTrack.snapshot` is or whether it matches the top card. On any other route the mini-player follows UI-11                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Critical |
| UI-17 | On the user's first visit to `/explore` (when `localStorage["moc.explore.onboarded"]` is unset), an onboarding overlay is rendered above the top card with `role="dialog"` and `aria-modal="true"`. After the user activates the overlay's primary action ("Got it"), `localStorage["moc.explore.onboarded"] === "1"` and subsequent visits do not render the overlay                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | High     |
| UI-18 | The phase pill in the Explore topbar contains the literal text `"Discovering taste"` when `profile.phase === "discovery"`, `"Finding artists"` when `"artist-refinement"`, and the pill element is absent from the DOM when `"personalized"`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | High     |
| UI-19 | On `/explore`, exactly one card has `data-explore-position="top"` at any time; activating ♥ or ✕ (or completing a swipe past threshold) removes that attribute from the previous top card and applies it to the next card                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | High     |
| UI-20 | Navigating away from `/explore` to another tab and back does not reset the explore queue: the same items remain visible, no new `GET /api/explore/next` request is fired, and no new song starts playing automatically on return                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | High     |
| UI-21 | On `/explore`, whenever the audio engine for the current top card enters `failed` state — regardless of how the card was loaded (`playPreview`'s `/play/resolve`+`engine.load` path OR `loadPreview`'s pre-resolved-URL fast path) and regardless of which underlying failure produced it (the engine fired `error` on the `<audio>` element, the resolve HTTP itself rejected, or `/play/resolve` returned `streamUrl: null`) — the FE drops the stale entry from its in-memory resolve cache and re-issues `POST /api/play/resolve` for the same snapshot. When the resolve returns a non-null `streamUrl` for a snapshot still matching the live top card (UI-25), the FE invokes `loadPreview(snapshot, freshUrl)` to re-attempt playback. Each `(top snapshot, mount)` pair re-issues the resolve at most once: if that retry also produces no playable stream (null body, network rejection, or a second `error` from the engine after the retry's load), the engine remains in `failed` but the card is **not** removed from the deck (UI-30). A healthy first load (a `playing` transition) clears the retry latch                                                                                                                                                                                                                                                                                                                                                                                 | Critical |
| UI-22 | On `/explore`, while the top card is playing, the FE refreshes the cached resolved stream URL for the **next-in-queue** snapshot (the item at index 1 in the live queue) once the current track is within `lookaheadMs` of its `durationMs` — concretely once `playableHandoffDecision({ progressMs, durationMs, lookaheadMs })` (LOGIC-23) first flips to `true` for that current track. The refresh fires `POST /api/play/resolve` for the next snapshot and replaces the cache entry with the returned URL; the refresh fires at most once per `(currentSnapshot, nextSnapshot)` pair (swapping either resets the latch); a failed or null-yielding refresh is silently swallowed and does not interrupt the currently-playing track, change the queue order, or block the next handoff (which falls through to UI-21's on-error recovery if its turn finds the cache still stale)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | High     |
| UI-23 | On `/explore`, when `fetchNext` returns `buildingQueue: true`, the UI renders the existing `RefillingState` (the empty-deck loading visual) and **polls `GET /api/explore/next` every 5 seconds** (no jitter, no exponential backoff) until either (a) the response contains at least one item — UI transitions to the live deck — or (b) the response returns `buildingQueue: false` AND `items.length === 0` — UI surfaces the "Try again" affordance (the existing 10-second timeout still applies as a safety net). The poll interval is cleared on every state transition that exits the building/empty branch and on component unmount. Because rebuild is idempotent per API-21, each poll is cheap — never spawns a duplicate build on the server                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Critical |
| UI-24 | _(deleted in favor of UI-30; the 5-second auto-skip-on-failure timer no longer exists, so there is nothing to suspend during a UI-21 retry. See UI-30 for the no-auto-skip-from-failure rule.)_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | -        |
| UI-25 | On `/explore`, when the UI-21 retry's `POST /api/play/resolve` resolves with a fresh `streamUrl`, the FE invokes `loadPreview(snapshot, freshUrl)` only if `snapshot` still matches the live top card (the item at index 0 of the queue) at the moment the response is received. If the user (or auto-skip) has already advanced the queue past `snapshot` while the retry was in flight, the fresh URL is silently discarded: `loadPreview` is not called, the in-memory resolve cache is not updated for `snapshot`, and the now-current top card's playback is not interrupted by the stale snapshot's audio                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Critical |
| UI-26 | On `/explore`, when the deck transitions to zero items while a queue rebuild is in flight (`buildingQueue === true`), the audio engine's `currentTrack` and the `navigator.mediaSession.metadata` are NOT cleared — they retain the last-loaded snapshot. The audio element is paused (so the previously-loaded track stops playing audibly) but the binding to the OS media session is preserved so the lock-screen / system media controls stay visible. When the next top card arrives from a subsequent poll, `useTopCardPreview` calls `loadPreview(newSnapshot)` which resumes playback through the same session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Critical |
| UI-27 | On `/explore`, when a card's cover-art `<img>` fires its `error` event (network failure, 404, decode error), the FE replaces the image with the token-driven `bg-border` placeholder div and the `aria-label` of the artwork container becomes `"Artwork unavailable"`. The error state is scoped per-card so a subsequent card with a different (still-loadable) `coverUrl` renders its image normally. The mini-player path uses the same placeholder rule, so an unloadable cover never surfaces the browser's broken-image icon                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | High     |
| UI-28 | On `/explore`, a transient `fetchNext` failure (non-2xx response, network rejection, schema parse failure) raised by a polling refresh does NOT clear the `buildingQueue` flag in the FE state. The polling effect stays subscribed at its 5 s cadence; the next successful poll either confirms `buildingQueue: true` (no transition) or correctly transitions to `false`. Only an explicit "Try again" from `RefillingState` (`retry()`) is allowed to reset state to a fresh probing path. Prior to this rule a single transient error during the build window silently halted polling until the user reloaded the page                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Critical |
| UI-29 | The audio loading path on `PlayerContext` (`playPreview`, `loadPreview`) does not gate `engine.load(snapshot, streamUrl)` on `requestAnimationFrame` callbacks firing. Concretely: when either function is invoked while `document.hidden === true` (a backgrounded tab on a desktop browser, where the UA suspends RAF), `engine.load` is reached within a microtask of the call regardless of whether any queued `requestAnimationFrame` callback ever fires. The 250 ms volume-dip crossfade is a visible-page enhancement only; while the page is hidden the fade is skipped and loading proceeds immediately. This is the secondary, defensive layer of the iOS lock-screen fix — the primary layer is UI-31 (sync Media Session handler), since iOS Safari does NOT reliably set `document.hidden = true` for a page that is actively rendering audio output even when the screen is locked, so this check alone does not cover the iOS case                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Critical |
| UI-31 | On `/explore`, the OS Media Session `nexttrack` / `previoustrack` action handlers (registered via `registerMediaOverrides` while ExplorePage is mounted) invoke `engine.load(nextSnapshot, streamUrl)` **synchronously inside the handler call** — before any React state update, useEffect cycle, `requestAnimationFrame` callback, or fade `.then()` chain. The handler reads the next-in-queue snapshot from a live items ref and its pre-resolved stream URL from the resolve cache populated by `useTopCardPreview`'s lookahead, then calls a non-fade `loadPreviewSync` on `PlayerContext` that calls `engine.load` directly; only after that does it trigger the async queue advancement (`swipe(direction)`). iOS Safari treats Media Session handlers as user gestures only for their synchronous portion: any async hop loses the gesture context and a subsequent `audio.play()` is rejected as "no user activation", which left the audio source pinned to the previous track until the user unlocked the device. Touch-driven swipes inside the visible page (✕ / ♥ buttons, manual drag past the threshold) continue to flow through the async `loadPreview` path with its 250 ms crossfade. When the pre-resolve cache misses (rare: `PRE_RESOLVE_AHEAD = 5` covers normal usage), the handler falls through to `swipe(direction)` only — the visible-page case still advances via the async fallback in `useTopCardPreview`, the lock-screen case won't audibly advance for that one swipe | Critical |
| UI-32 | Both the Explore swipe card cover and the Now Playing overlay render a button with accessible name `"Bad remix"`, built from the design-system `Button` (`variant="secondary"`, `size="sm"`) wrapping an `Icon` (`name="thumbs-down"`). Clicking the button does NOT navigate, does NOT unmount the player, and the `SongSnapshot` that was active before the click remains the active track in player state after the network call resolves (only the underlying stream URL / source track changes)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Major    |
| UI-30 | On `/explore`, the deck never advances as a result of the audio engine entering `failed` for the top card. No timer, no effect, and no code path other than an explicit user-driven swipe (the ✕ / ♥ buttons, a manual drag past the swipe threshold, or the OS media-session prev/next handlers wired by Explore — _all_ originating from a user gesture) is permitted to remove the top card from the queue while it is in `failed`. Concretely: an arbitrary amount of wall-clock time (≥ 10 s) may elapse with `engineState.status === "failed"` and the top card MUST remain at `items[0]`; the `<audio>` source MUST NOT have been swapped to the next-in-queue's URL; no `POST /api/explore/swipe` MUST have been fired by the FE. The auto-LIKE-on-`ended` flow (a track that played through to completion fires `swipe("right")`) is preserved and is the only programmatic queue advancement that remains — it is gated on the engine reaching `ended`, never `failed`. Closes the production bug where a stale signed stream URL on a freshly-resolved snapshot 403'd in the browser, the 5-s timer fired, and the user saw the card silently vanish without ever taking an action on it                                                                                                                                                                                                                                                                                                        | Critical |

**Test files:** `tests/invariants/ui/auth.test.tsx`, `tests/invariants/ui/nav.test.tsx`, `tests/invariants/ui/search.test.tsx`, `tests/invariants/ui/sign-in-modal.test.tsx`, `tests/invariants/ui/player.test.tsx`, `tests/invariants/ui/player-visibility.test.tsx`, `tests/invariants/ui/explore-media-session.test.tsx`, `tests/invariants/ui/now-playing.test.tsx`, `tests/invariants/ui/explore.test.tsx`, `tests/invariants/ui/explore-preresolve.test.tsx`, `tests/invariants/ui/explore-robustness.test.tsx`

---

## SEC — authorization and credential hygiene

| ID     | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Severity |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| SEC-01 | The session cookie value, the `oauth_state` cookie value, the `SESSION_SECRET`, and the `GOOGLE_CLIENT_SECRET` never appear in any HTTP response body or structured log line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Critical |
| SEC-02 | `GET /api/auth/google/callback` returns a 4xx error when the `state` query param is missing or does not match the value in the `oauth_state` cookie                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Critical |
| SEC-03 | Routes outside the public allowlist (`GET /health`, `GET /api/auth/google`, `GET /api/auth/google/callback`, `POST /api/auth/logout`, `POST /api/search`, `POST /api/play/resolve`) return 401 without a valid session cookie                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Critical |
| SEC-04 | `GENIUS_ACCESS_TOKEN` never appears in any HTTP response body at any route, whether or not the request succeeds                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Critical |
| SEC-05 | `GET /api/search/history` for user A never returns entries owned by user B; the endpoint scopes all results to the authenticated session's `userId`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Critical |
| SEC-06 | `interest_scores` documents are scoped per-user: events written by user A never appear in any HTTP response served to user B, and the repository filters every read by the authenticated `userId`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Critical |
| SEC-07 | The configured `SOUNDCLOUD_USER_AGENT` value and the SoundCloud `client_id` extracted from upstream HTML never appear in any HTTP response body served by `apps/api`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Critical |
| SEC-08 | `POST /api/play/started` and `POST /api/play/completed` always derive `userId` from the authenticated session; any `userId` field present in the request body is ignored, so user A cannot write to user B's `listening_events` or `interest_scores`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Critical |
| SEC-09 | `POST /api/explore/swipe` always derives `userId` from the authenticated session; any `userId` field present in the request body is ignored, so user A cannot write to user B's `swipes` or `interest_scores` (extends SEC-08)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Critical |
| SEC-10 | `GET /api/explore/profile` for user A never returns user B's `taste_profiles` document; the repository scopes every read by the authenticated `userId` (extends SEC-06)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Critical |
| SEC-11 | `GET /api/explore/next` for user A never returns items from user B's `explore_queue`; the queue read used to serve `/next` filters by the authenticated session's `userId` (extends SEC-10)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Critical |
| SEC-12 | `GET /api/me/taste/profile` for user A never returns any `buckets` document whose `userId !== A`; the `buckets` repository filters every read by the authenticated session's `userId` (extends SEC-06 / SEC-10 / SEC-11 — all reads scoped per-user)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Critical |
| SEC-13 | No code path writes a `context_scores` or `bucket_song_scores` row whose `userId` differs from the authenticated request's session `userId`. The scoring service derives `userId` from the caller (the request-scoped value already validated by `SEC-08`/`SEC-09`) and stamps every upsert / set with that same value; any body- or query-supplied `userId` is ignored upstream. The `(userId, songKey, axis, value)` uniqueness of `context_scores` (DATA-17) is therefore tenant-scoped — user A's swipes never overwrite user B's rows                                                                                                                                                                                                                                                    | Critical |
| SEC-14 | The Explore queue's slot-eligibility filter (API-25) reads swipes via `SwipesRepository.findSwipesForUser(userId)`, where `userId` is derived from the authenticated session. User A's `swipes` documents therefore never influence user B's `GET /api/explore/next` eligibility decisions — A's "Tuesday-evening burnt" history cannot mask a track for B. Restates the read side of SEC-09 (which covered the write side) for the eligibility-filter path                                                                                                                                                                                                                                                                                                                                   | Critical |
| SEC-15 | The auto-bucket builder reads only data belonging to the authenticated `userId` (positive-signal swipes via `SwipesRepository.findSwipesForUser(userId)`, interest scores via `InterestScoresRepository.findScoresForUser(userId)`, existing buckets via `BucketsRepository.findForUser(userId)`) and writes only `buckets` and `bucket_song_scores` rows stamped with the same `userId`. No row that the builder reads or writes ever contains another user's data, and the `userId` in every write comes from the caller's argument (not from LLM output, request body, or any field parsed from external input)                                                                                                                                                                            | Critical |
| SEC-16 | `POST /api/me/taste/custom-mix` always derives `userId` from the authenticated session; any `userId`-shaped field present in the request body is ignored. The created `buckets` row and `custom_mix_jobs` row both carry `userId === session.user.uid`. The custom-mix builder reads only data belonging to that `userId` (positive-signal swipes, interest scores, context scores, bucket scores, existing buckets — every read goes through a `userId`-scoped repository method) and writes only `buckets`, `bucket_song_scores`, and `custom_mix_jobs` rows stamped with that same `userId`. User A's mix is therefore never returned to user B via `GET /api/me/taste/profile`, and user A's `custom_mix_jobs` document is never written with user B's `userId` (extends SEC-09 / SEC-15) | Critical |
| SEC-17 | Skip decrements only modify `bucket_song_scores` rows whose `userId` matches the authenticated session's `userId` (extends SEC-13). The `PlayEventsService` derives `userId` from the request-scoped value validated by `SEC-08`; that same `userId` is passed to every `BucketSongScoresRepository.inc()` call in the skip path. No skip decrement can be triggered for another user's rows by any body parameter or external input — the `bucketId` received from the client is validated to belong to the authenticated user's session before any decrement is issued                                                                                                                                                                                                                      | Critical |

**Test files:** `tests/invariants/sec/auth.test.ts`, `tests/invariants/sec/search.test.ts`, `tests/invariants/sec/play.test.ts`, `tests/invariants/sec/explore.test.ts`, `tests/invariants/sec/taste.test.ts`, `tests/invariants/sec/scoring.test.ts`, `tests/invariants/sec/bucket-builder.test.ts`, `tests/invariants/sec/custom-mix.test.ts`, `tests/invariants/sec/skip-tracking.test.ts`

---

## PRIVACY — data flow boundaries

| ID         | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Severity |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| PRIVACY-01 | Outgoing HTTP requests to Audius, Deezer, Radio Browser, and Genius carry only the search query string; no user identifier, session token, or IP forwarding header is added by our aggregator code                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Critical |
| PRIVACY-02 | `search_history` content (query strings and `userId` mappings) never leaves the database tier; the search aggregator (providers, cache) is entirely unaware of history — no history data reaches third-party APIs or LLM prompts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Critical |
| PRIVACY-03 | Outgoing HTTP requests fired by the `/api/play/resolve` flow (Audius stream redirect, SoundCloud HTML scrape, SoundCloud transcoding API) carry only the snapshot fields and our spoofed User-Agent; no user identifier, session cookie, or `X-Forwarded-*` header is added by our resolver code                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Critical |
| PRIVACY-04 | `POST /api/play/started` and `POST /api/play/completed` make no outgoing third-party HTTP request; listening events stay in the database tier and never reach providers, telemetry, or LLM prompts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Critical |
| PRIVACY-05 | The browser's audio-fetch URL set on `<audio src>` is the raw stream URL from the resolver response; the FE code adds no user-identifier, session, or fingerprinting query parameters to it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Critical |
| PRIVACY-06 | Outgoing HTTP requests made by the SoundCloud search provider (the `soundcloud.com/search/sounds*` HTML scrape and the `api-v2.soundcloud.com/search/tracks` JSON call) carry only the query string and our spoofed `SOUNDCLOUD_USER_AGENT`; no user identifier, session cookie, or `X-Forwarded-*` header is added by our aggregator code (extends PRIVACY-01)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Critical |
| PRIVACY-07 | `POST /api/explore/swipe` makes no outgoing third-party HTTP request; swipes stay in the database tier and never reach providers, telemetry, or LLM prompts (mirrors PRIVACY-04)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Critical |
| PRIVACY-08 | The taste-profile build prompt's user message is a pure function of `(recentSwipes, recentListens, previousSummary)` read from our DB — `userId`, `email`, IP, session cookie, and any field outside that explicit set never appear anywhere in the rendered system prompt or user message bodies                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Critical |
| PRIVACY-09 | The explore-personalized prompt's user message is a pure function of `(profile, scoreBuckets, candidatePool)` derived from our DB — `userId`, `email`, IP, session cookie, and raw swipe-direction history never appear anywhere in the rendered system prompt or user-message bodies. The projected fields are: from `profile` only `(genres, artists, summaryText, tempoBucket, remixPreference)`; from `scoreBuckets` only `{title, artist}` per entry; from `candidatePool` only `{title, artist, source}` (mirrors PRIVACY-08 for the queue personalized-sourcing step)                                                                                                                                                                                                                                                                                                             | Critical |
| PRIVACY-10 | The explore-artist-refinement prompt's user message is a pure function of `(profile, candidatePool)` derived from our DB — `userId`, `email`, IP, session cookie, and raw swipe-direction history never appear anywhere in the rendered system prompt or user-message bodies. The projected fields are: from `profile` only `(genres, artists, summaryText, tempoBucket, remixPreference)`; from `candidatePool` only `{title, artist, source}` (mirrors PRIVACY-09 for the queue artist-refinement-sourcing step)                                                                                                                                                                                                                                                                                                                                                                       | Critical |
| PRIVACY-11 | The explore-cold-start prompt's user message is a pure function of `(recentSwipes)` derived from our DB. When `recentSwipes` is empty (first call), the prompt body is the legacy seed-curation text and carries no per-user data. When `recentSwipes` is non-empty, only the projected fields `{title, artist, direction}` per swipe reach the prompt body — `userId`, `email`, IP, session cookie, and swipe timestamps / snapshot fields beyond `(title, artist)` never appear in the rendered system prompt or user-message bodies                                                                                                                                                                                                                                                                                                                                                   | Critical |
| PRIVACY-12 | `GET /api/me/taste/profile` makes no outgoing third-party HTTP request and no LLM call: the taste module's controller, service, and repositories contain no `fetch(…)` invocation, no `http(s)://` URL, no `node:http`/`node:https` import, and no Anthropic-SDK import. Bucket reads stay strictly in the database tier (mirrors PRIVACY-04 / PRIVACY-07 for taste-profile reads)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Critical |
| PRIVACY-13 | Context-score writes (the scoring service hooked off `/api/explore/swipe`, `/api/play/started` / `/api/play/completed`, and the search `saved` event) make no outgoing third-party HTTP request and no LLM call: the scoring service's source file and its dependencies (`@moc/api-core` taste helpers, the `context_scores` repository) contain no `fetch(…)` invocation, no `http(s)://` URL, no `node:http`/`node:https` import, and no Anthropic-SDK import. Every contextual-scoring side effect stays strictly in the database tier (mirrors PRIVACY-04 / PRIVACY-07 / PRIVACY-12)                                                                                                                                                                                                                                                                                                 | Critical |
| PRIVACY-14 | The auto-bucket build prompt's user message is a pure function of `(recentSongs, existingBuckets)` derived from our DB — `userId`, `email`, IP, session cookie, swipe direction, swipe timestamps, and any field outside the explicit projection (`songKey`, `title`, `artist`, `kind` from each song; `name`, `description` from each bucket) never appear anywhere in the rendered system prompt or user-message bodies. The `buildBucketPrompt` pure helper (AI-11) accepts only `{ recentSongs, existingBuckets }` and has no `userId` parameter, so leakage is structurally impossible rather than just convention-enforced                                                                                                                                                                                                                                                         | Critical |
| PRIVACY-15 | The custom-mix build prompt's user message is a pure function of `(promptText, pool, buckets)` — `promptText` is the user's submitted string passed through verbatim (the user controls its content; we treat it as opaque input and do not parse identity out of it), `pool` is `{ songKey, title, artist, kind, generalScore }` per touched song from our DB, and `buckets` is `{ id, name, description }` per existing bucket from our DB. `userId`, `email`, IP, session cookie, swipe direction, swipe timestamps, raw context-score rows, and any field outside this explicit projection never appear anywhere in the rendered system prompt or user-message bodies. The `buildCustomMixPrompt` pure helper (AI-14) accepts only `{ promptText, pool, buckets }` and has no `userId` / `email` / session parameter, so leakage of those identity fields is structurally impossible | Critical |
| PRIVACY-16 | The skip detector in `PlayEventsService` makes no outgoing HTTP call to any third-party service and never invokes the LLM. Its only external I/O is reading `custom_mix_jobs` and writing `bucket_song_scores` via their respective repositories (DB tier only). The `play-events.service.ts` source file and its imports contain no `fetch(…)` invocation, no `http(s)://` URL, no `node:http`/`node:https` import, and no Anthropic-SDK import (mirrors PRIVACY-04 / PRIVACY-13 for the skip-decrement path)                                                                                                                                                                                                                                                                                                                                                                           | Critical |

**Test files:** `tests/invariants/privacy/search.test.ts`, `tests/invariants/privacy/play.test.ts`, `tests/invariants/privacy/player-web.test.ts`, `tests/invariants/privacy/explore.test.ts`, `tests/invariants/privacy/taste-profile.test.ts`, `tests/invariants/privacy/explore-personalized.test.ts`, `tests/invariants/privacy/explore-artist-refinement.test.ts`, `tests/invariants/privacy/explore-cold-start.test.ts`, `tests/invariants/privacy/taste.test.ts`, `tests/invariants/privacy/scoring.test.ts`, `tests/invariants/privacy/skip-tracking.test.ts`, `tests/invariants/ai/auto-bucket.test.ts`, `tests/invariants/ai/custom-mix.test.ts`

---

## AI — LLM and embedding contracts

| ID    | Invariant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Severity |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| AI-01 | The `buildTastePrompt` pure helper produces a `(system, userMessage)` pair whose serialized bytes never contain the substring of any `userId`, `email`, IP address, session token, or other identity field passed to the build pipeline — it only ever sees `recentSwipes`, `recentListens`, and `previousSummary`, and only the snapshot fields `(title, artist, kind)` from each swipe / listen reach the prompt                                                                                                                                                                                                                                                                                                | Critical |
| AI-02 | `buildTastePrompt` is deterministic: equal `(recentSwipes, recentListens, previousSummary)` inputs produce byte-identical `(system, userMessage)` pairs, so two users with identical inputs derive the same prompt-cache key (which the Anthropic SDK derives from the system prompt + user-message bytes — never from user identity)                                                                                                                                                                                                                                                                                                                                                                             | Critical |
| AI-03 | `buildTastePrompt` enforces a bounded prompt: at most `N=200` swipes and `M=100` listens reach the user message, and `previousSummary` is truncated to ≤ 4 KB. Inputs above the limit are dropped newest-first (the most recent entries are always retained), and the helper never throws on oversized inputs                                                                                                                                                                                                                                                                                                                                                                                                     | Critical |
| AI-04 | The `buildPersonalizedPrompt` pure helper produces a `(system, userMessage)` pair whose serialized bytes never contain any `userId`, `email`, IP address, session token, or raw swipe-direction history — it only ever sees `(profile, scoreBuckets, candidatePool)`, and only the projected fields (`genres`, `artists`, `summaryText`, `tempoBucket`, `remixPreference` from profile; `{title, artist}` per score-bucket entry; `{title, artist, source}` per candidate) reach the prompt body                                                                                                                                                                                                                  | Critical |
| AI-05 | `buildPersonalizedPrompt` is deterministic: equal `(profile, scoreBuckets, candidatePool)` inputs produce byte-identical `(system, userMessage)` pairs, so two users with identical inputs derive the same prompt-cache key (the SDK derives the key from system + user-message bytes — never from user identity). Callers performing pre-prompt randomization (e.g. the 5-of-N artist shuffle) are responsible for producing the same already-shuffled `profile` if they want cache hits across calls                                                                                                                                                                                                            | Critical |
| AI-09 | The `buildArtistRefinementPrompt` pure helper produces a `(system, userMessage)` pair whose serialized bytes never contain any `userId`, `email`, IP address, session token, or raw swipe-direction history — it only ever sees `(profile, candidatePool)`, and only the projected fields (`genres`, `artists`, `summaryText`, `tempoBucket`, `remixPreference` from profile; `{title, artist, source}` per candidate) reach the prompt body. Determinism mirrors AI-05 — equal `(profile, candidatePool)` inputs produce byte-identical output                                                                                                                                                                   | Critical |
| AI-10 | The `buildColdStartPrompt` pure helper produces a `(system, userMessage)` pair whose serialized bytes never contain any `userId`, `email`, IP address, or session token — even when called with the soft-signal `recentSwipes` shape, it only sees `{title, artist, direction}` per swipe and emits exactly those fields into the prompt body. The user's email, IP, and session cookie cannot leak through this path. Raw swipe direction _does_ reach the prompt (it's the entire point of the soft signal), but is paired with title + artist only — no timestamps, no per-swipe identifiers, no snapshot fields beyond title + artist. Determinism mirrors AI-05 — equal inputs produce byte-identical output | Critical |

| AI-06 | The Anthropic SDK construction routes the configured credential by prefix: values starting with `sk-ant-oat01-` (OAuth tokens, e.g. issued by `claude setup-token`) are passed via the SDK's `authToken` option so Anthropic authenticates them via `Authorization: Bearer …`; values matching the standard API-key shape `sk-ant-api…` are passed via `apiKey` so Anthropic authenticates them via `x-api-key`. The pure helper additionally sets the **unused** auth field to `null` (not undefined) — `{ authToken: …, apiKey: null }` for OAuth tokens, `{ apiKey: …, authToken: null }` for API keys — to suppress the SDK's `readEnv()`-based fallback that would otherwise auto-load `process.env.ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` into the omitted field and silently send two conflicting auth headers (e.g. an OAuth token in both `x-api-key` AND `Authorization: Bearer`, which Anthropic rejects with `401 invalid x-api-key`). The decision depends only on its argument | Critical |
| AI-07 | Module bootstrap fails fast when the configured Anthropic credential is empty, missing, whitespace-only, or does not match a recognized credential shape (`sk-ant-api…` API key or `sk-ant-oat01-…` OAuth token). The thrown error names the env var and lists the two accepted shapes. This prevents the LLM-driven explore paths (cold start, rerank, taste-profile build) from silently 401-ing and falling back to seed snapshots when the credential is misconfigured | Critical |
| AI-08 | When an `AnthropicClient.complete` call against the configured primary model fails with a `rate_limit_error` (HTTP 429 — observed in production with `sk-ant-oat01-…` OAuth tokens, where the subscription's premium-model fallback policy 429s Sonnet/Opus while leaving Haiku flowing), the client transparently retries the same request once against the configured fallback model. Non-rate-limit errors are surfaced to the caller unchanged. The decision is made by a pure helper that depends only on the attempt index, the primary model name, and the fallback model name — when the two names are equal or the fallback is empty, no retry occurs. A second pure helper recognizes the 429 / `rate_limit_error` shape without depending on SDK class identity, so changes to the SDK's error hierarchy do not silently disable the fallback | Critical |
| AI-11 | The `buildBucketPrompt` pure helper produces a `(system, userMessage)` pair whose serialized bytes never contain any `userId`, `email`, IP address, session token, swipe direction, swipe timestamp, or any field outside the explicit projection (`songKey`, `title`, `artist`, `kind` per song; `name`, `description` per existing bucket). The helper has no `userId` parameter — leakage is structurally impossible. Only the song and bucket fields listed above ever reach the prompt body | Critical |
| AI-12 | `buildBucketPrompt` is deterministic: equal `(recentSongs, existingBuckets)` inputs produce byte-identical `(system, userMessage)` pairs, so two users with identical inputs derive the same prompt-cache key (the Anthropic SDK derives the cache key from the system prompt + user-message bytes — never from user identity) | Critical |
| AI-13 | `buildBucketPrompt` enforces a bounded prompt: at most `N=20` songs reach the user message. Inputs above the limit are dropped newest-first (the most recent entries are retained), and the helper never throws on oversized inputs. The small cap exists because the bucket-builder is an **incremental** worker (LOGIC-38) — each run only considers songs the LLM has not yet bucketed for this user, so 20 newest-unbucketed is a worst-case-per-run bound. Keeping the output JSON small also keeps the LLM well below its `max_tokens` cap, so the response is never truncated mid-object (the failure mode that caused `auto_bucket_build_failed reason=llm_parse_failed err=SyntaxError: no JSON object found in bucket-builder response` in production when the prior cap was 300) | Critical |
| AI-14 | The `buildCustomMixPrompt` pure helper produces a `(system, userMessage)` pair whose serialized bytes never contain any `userId`, `email`, IP address, or session token — it only ever sees `{ promptText, pool, buckets }`, and only the projected fields (`songKey`, `title`, `artist`, `kind`, `generalScore` per pool song; `id`, `name`, `description` per existing bucket) plus the user-supplied `promptText` reach the prompt body. The helper has no `userId` parameter — identity leakage is structurally impossible. `promptText` is opaque user input (the user controls its content) and is passed through verbatim, but no parsing extracts identity fields from it | Critical |
| AI-15 | `buildCustomMixPrompt` is deterministic: equal `(promptText, pool, buckets)` inputs produce byte-identical `(system, userMessage)` pairs, so two callers with identical inputs derive the same prompt-cache key (the Anthropic SDK derives the cache key from the system prompt + user-message bytes — never from user identity) | Critical |
| AI-16 | `buildCustomMixPrompt` enforces a bounded prompt: `promptText` longer than 500 chars is rejected upstream (API-26's 400 response) before reaching the helper; at most `N=400` songs reach the user message (input above the cap is dropped newest-first); `description` per bucket entry that exceeds 200 chars is truncated; the helper never throws on oversized song-pool inputs | Critical |

**Test files:** `tests/invariants/ai/taste-profile.test.ts`, `tests/invariants/ai/explore-personalized.test.ts`, `tests/invariants/ai/explore-artist-refinement.test.ts`, `tests/invariants/ai/explore-cold-start.test.ts`, `tests/invariants/ai/anthropic-client.test.ts`, `tests/invariants/ai/auto-bucket.test.ts`, `tests/invariants/ai/custom-mix.test.ts`

---

## PWA — installability and offline behavior

| ID     | Invariant                                                                                                                                                                                                                                                                                                                                                                           | Severity |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| PWA-01 | Refreshing the browser on `/explore`, `/taste`, or `/search` rehydrates to the same route — the matching bottom-nav tab is highlighted after rehydration                                                                                                                                                                                                                            | High     |
| PWA-02 | When playback is active and `navigator.mediaSession` is available, `mediaSession.metadata.title`, `.artist`, and `.artwork` reflect the current track's snapshot, and action handlers `play`, `pause`, and `previoustrack` are registered (rebound when the current track changes); when the API is absent (older Safari, jsdom) the player still mounts and plays without throwing | High     |

**Test files:** `tests/invariants/pwa/routing.test.ts` (Layer 3 — Playwright, stubs pending), `tests/invariants/pwa/media-session.test.tsx`

---

## BROWSER — verified by Layer 3 (Playwright)

| ID         | Invariant                                                                                                                                                                                                                                                                                               | Severity |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| BROWSER-01 | On a 375×667 mobile viewport, each bottom-nav tab has a touch target ≥ 44×44 px and the nav applies `env(safe-area-inset-bottom)` so no content is occluded by the iOS home indicator                                                                                                                   | High     |
| BROWSER-02 | On a 375×667 mobile viewport, the search input is visible at the top of the viewport without scrolling; results are scrollable below it; the bottom nav remains fixed and does not occlude the last result                                                                                              | High     |
| BROWSER-03 | On a 375×667 mobile viewport, the mini-player + bottom nav together fit within the bottom region without horizontal scroll; the play/pause and dismiss buttons have a touch target ≥ 44×44 px                                                                                                           | High     |
| BROWSER-04 | On a 375×667 mobile viewport, the now-playing overlay fits without horizontal scroll, the cover-art square is ≥ 240×240 px, every transport button (skip-back, play/pause, skip-forward) has a touch target ≥ 44×44 px, and the collapse-chevron and more-overflow buttons in the topbar do not overlap | High     |
| BROWSER-05 | On a 375×667 mobile viewport, each `IconButton` in the Explore action row (pass / pause / like) has a touch target ≥ 44×44 px and the row itself fits without horizontal scroll; the explore card stack also fits without horizontal scroll and the topmost card's artwork area is at least 240×240 px  | High     |
| BROWSER-06 | All four Explore page states (`default`, `mid-swipe-right`, `refilling`, `onboarding`) pass `expectAccessible(page)` (axe-core WCAG 2.1 AA — contrast, labels, ARIA) on a 375×667 viewport                                                                                                              | High     |

**Test files:** `tests/invariants/browser/bottom-nav.test.ts`, `tests/invariants/browser/search.test.ts`, `tests/invariants/browser/player.test.ts`, `tests/invariants/browser/now-playing.test.ts`, `tests/invariants/browser/explore.test.ts` (Layer 3 — Playwright)

---

## Adding invariants for new features

When you start a feature:

1. Identify what the invariant constrains — data shape, function contract, HTTP, DOM, security, privacy, AI, PWA, or browser-only.
2. Append a row to the matching category above with the next available ID (e.g. `DATA-02`, `LOGIC-01`).
3. Stub the test in the relevant `tests/invariants/<category>/*.test.ts` file using `describe("<ID>: <description>", ...)`.
4. Confirm the test fails (red) before implementing.
5. Only then implement the feature.

Run `/new-invariant` for a guided walkthrough.
